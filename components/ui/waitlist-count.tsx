// This component was removed in favor of a static, stable display.
// Keeping a minimal component to avoid imports breaking until the dynamic build cache is cleared.
export default function WaitlistCountStub() {
  return <>{"2.1K"}</>;
}
